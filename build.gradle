group 'spbau'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'jacoco'

allprojects {
    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.jetbrains', name: 'annotations', version: '13.0'
    testCompile group: 'junit', name: 'junit', version: '4.11'
    testCompile "org.mockito:mockito-core:2.+"
}

jacocoTestReport {
    reports {
        xml.enabled true
        html.enabled false
    }
}

task jarClient(type: Jar) {
    manifest {
        def manifest = "${projectDir}/src/main/resources/ru/spbau/sazanovich/nikita/client/MANIFEST.MF"
        from file(manifest)
    }
    baseName = "client"
    from {
        configurations.compile.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
    with jar
}

task jarServer(type: Jar) {
    manifest {
        def manifest = "${projectDir}/src/main/resources/ru/spbau/sazanovich/nikita/server/MANIFEST.MF"
        from file(manifest)
    }
    baseName = "server"
    from {
        configurations.compile.collect {
            it.isDirectory() ? it : zipTree(it)
        }
    }
    with jar
}